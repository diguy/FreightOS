# 项目协作与交付工作流

本文档是本项目的全局协作规范。新窗口开始工作时，应同时阅读：

1. `prd.md`：产品目标和需求范围。
2. `ARCD.md`：系统架构和模块边界。
3. `project.md`：当前阶段、验证事实、难点和下一步。
4. `WORKFLOW.md`：本文件，规定每次工作的执行方式。

## 1. 小切片原则

每次只做一个可以独立验证的小切片。

一个小切片应当：

- 有明确的单一目标。
- 有明确的验收标准。
- 只触及完成目标所需的最少文件。
- 可以通过聚焦测试、脚本检查或人工验证判断是否完成。
- 完成后不会留下半完成的跨模块改动。

不在一个切片中同时进行大规模重构、多个业务模块开发、基础设施迁移和文档重写。

## 2. 开始前说明

开始修改前，必须先向用户说明：

- 本次切片的目标。
- 验收标准。
- 准备修改的文件。
- 如果涉及外部服务，说明需要的运行条件和手工验收方式。

如果发现需求存在会改变行为的歧义，先说明采用的解释和假设，再开始编辑。

## 3. 实施顺序

每个切片按以下顺序执行：

1. 读取相关代码、测试、契约和当前 Git 状态。
2. 确认本次切片的边界。
3. 修改最少的必要文件。
4. 先运行聚焦测试或最小验证。
5. 根据影响范围运行集成测试或全量测试。
6. 检查差异和提交边界。
7. 只提交本次切片相关文件。
8. 汇报结果、限制和 Git 阶段版本。

当完整回归中只有少量失败时，先完成全部用例并统一归类问题。修正后，
根据失败类型选择少量代表性用例验证修复效果；只有在问题跨越多个模块、
无法按类别隔离，或代表性验证通过后需要确认整体没有回归时，才重新运行
完整回归。不得因为少量失败就默认重复消耗完整外部模型调用额度。

## 4. Git 提交规则

完成验证后，只提交本次切片相关文件。必须保留用户已有的 dirty worktree，不得因为提交方便而把其他改动一起提交。

提交前检查：

```powershell
git status --short
git diff --check
git diff --cached --name-status
```

提交信息和阶段版本名保持一致或高度一致。

## 5. 阶段版本命名

每次提交后必须给出一个可读的阶段版本名，格式为：

```text
主阶段.步骤（本切片名称）
```

例如：

```text
9.2（工单状态查询端到端验收）
```

版本名用于表达当前模块和工作阶段，便于用户进行 Git 回退和分支开发。后续可以基于某个版本派生新路线，例如：

```text
7.2
7.2b
```

当前已完成的上下文基线为：

```text
1.1（项目上下文基线）
```

当前工作流规范切片为：

```text
1.2（项目协作流程规范）
```

## 6. 完成汇报格式

每次切片完成后，至少汇报：

1. 做了什么。
2. 修改了哪些文件。
3. 执行了哪些验证命令以及结果。
4. 尚存的风险或限制。
5. 下一步建议。
6. 阶段版本名和对应 Git 提交。

## 7. 代码和外部系统约束

- 不修改 `vendor/`，除非用户明确要求。
- 不覆盖或删除用户已有改动。
- Dify DSL 修改必须创建新的版本文件，保留旧版本。
- 业务行为变化必须有对应的聚焦测试。
- 外部 Dify、RAGFlow、MySQL、Redis 或 MCP 的 fake 测试不能替代真实环境验收；需要同时记录手工验证步骤。
- `IntentResult` 只表示当前轮结果，跨轮数据放入会话状态。
- Dify 负责当前轮意图识别，Python Agent 负责状态、校验和编排，业务 Service 负责业务事实。

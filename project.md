# 当前项目阶段

> 新窗口必须先阅读 `AGENTS.md`、`WORKFLOW.md`、`prd.md`、`ARCD.md` 和本文件。

## 0. 当前产品化阶段

截至 2026年9月8日，项目已经完成后端智能客服 MVP 和消费者端核心链路的主要验收，
当前进入第一阶段生产化收尾。当前不是从零开始做系统，而是在已验证的 Dify、
FastAPI、Python Agent、MySQL、Redis 和 RAGFlow 链路上补齐生产化能力。

第一阶段优先级：消费者电脑网页。

第一阶段交付目标：

1. 手机号验证码登录。
2. 客户自然语言客服对话。
3. 订单物流查询。
4. 地址修改申请，并在用户明确确认后创建工单。
5. 投诉申请，并在用户明确确认后创建工单。
6. 工单号和工单状态查询。
7. 最近会话恢复和跨轮状态安全隔离。

第一阶段暂不包含物流公司后台、实时在线人工、消费者文档上传、完整订单中心和手机端专门适配。物流公司后台与 PDF/Markdown 知识库上传属于第二阶段。

## 0.1 已确定的前端技术

第一阶段消费者端确定采用：

- `shadcn/ui`：白底蓝色业务系统的界面组件。
- `assistant-ui`：客服聊天界面和多轮消息交互。
- `TanStack Query`：FastAPI 数据请求、缓存、刷新和错误处理。

当前只确定技术选型，不代表已经安装或完成前端实现。引入前仍需建立前端目录、锁定版本、检查许可证和依赖兼容性，并通过最小页面验证。

## 1.1 当前产品差距

已有：

- Dify 当前轮意图识别和结构化输出。
- Python Agent 解析、会话槽位合并、确认门控和工具安全控制。
- 订单物流、知识问答、地址修改、投诉、人工工单和工单状态查询。
- MySQL 业务数据、Redis 会话存储、RAGFlow 检索。
- 真实链路和自动化测试已通过，最近聚焦测试为 38 passed，全量测试为 202 passed。

尚缺：

- 投诉、人工和外部服务异常场景的 Dify v6 全真实回归，以及生产级结构化观测和告警。
- 当前手机号验证码仍是开发演示认证，不是真实短信认证和生产级令牌。
- 最近会话目前主要保存在浏览器本地，尚未完成服务端会话索引和跨设备恢复。
- 生产级错误提示、加载状态、权限失效和登录过期处理。
- 第二阶段物流公司后台、角色权限、多公司隔离和知识文档上传。

## 1.2 阶段完成标准

第一阶段只有同时满足以下条件才算完成：

- 用户可以通过手机号验证码登录电脑网页。
- 理解阶段使用当前问题、历史对话、当前槽位和系统提示词完成意图判断。
- 回答阶段使用当前问题、历史对话、有效会话上下文、工具结果和回答提示词生成
  `final_answer`。
- 用户看不到后端 JSON、内部字段名或 Python 字典，客服回答统一为自然语言。
- 用户可以连续多轮查询自己的订单和工单。
- 地址修改只允许符合状态规则的订单，并且必须经过明确确认。
- 投诉必须收集必要信息并经用户确认后创建。
- 用户无法访问其他用户的订单、工单或会话。
- 关键流程具备自动化测试和真实环境验收记录。
- 出现 Dify、RAGFlow 或业务服务异常时，用户得到可理解的降级提示。

## 1. 当前结论

当前日期：2026-09-08。

项目已完成 MVP 核心能力和主要真实环境验收，现进入消费者端产品化阶段。
不应再把工单状态查询、RAGFlow 或 MCP 描述为“尚未开始”。

已完成模块：

- Dify v6 双阶段 Chatflow：理解阶段输出结构化意图，回答阶段输出客户可见的
  `final_answer`。
- Python Agent 输出解析、会话状态、槽位合并、确认和工具门控。
- 订单物流查询。
- 工单创建、工单状态查询、权限和状态流转。
- RAGFlow 经 FastAPI 中间层的真实知识检索。
- MySQL 业务数据、Redis 会话存储、迁移、健康检查和备份脚本。
- MCP 六项业务工具及 stdio 协议入口。
- 消费者端 React 聊天页面、真实客服 API 接入、开发演示登录和浏览器本地会话恢复。

当前已完成：

- Python 对 `final_answer` 的校验、内部 JSON 防泄漏和确定性安全兜底。
- 已完成业务与待处理任务隔离；未知或模糊消息只在存在待处理任务时继续提示
  缺失信息或确认，不会从已完成业务恢复上下文。

当前明确未完成：

- 投诉、人工、外部异常的 Dify v6 全真实回归。
- 真实短信认证、生产级令牌、服务端会话索引和跨设备恢复。
- 登录过期、权限失效和生产级错误体验。

## 2. 当前真实运行环境

- 默认业务数据库：MySQL，`127.0.0.1:3308`。
- 默认会话存储：Redis，`127.0.0.1:6380`。
- FastAPI：`http://localhost:8000`。
- Dify API：`http://localhost/v1`。
- Dify Docker 调用后端：`http://host.docker.internal:8000/api/v1/agent/turn`。
- 测试用户：`demo-user-001`。
- RAGFlow 真实 Endpoint、API Key 和知识库 ID 只从本机 `.env` 读取，不写入文档或回复。

开始任何数据测试前，必须从当前 MySQL 查询并核对订单号、工单号、用户归属、
状态和关联关系。历史交接文档、旧报告和示例编号不能作为数据库事实。

## 3. 已验证事实

### 3.1 默认环境全量测试

执行：

```powershell
poetry run pytest tests -q
```

结果：

```text
202 passed in 19.35s
```

### 3.2 真实 Dify 链路

执行：

```powershell
poetry run pytest tests/test_chat_service.py tests/test_chat_route.py -q
poetry run python scripts/test_live_dify_chain.py --message "查一下 ORD1001 到哪里了" --session-id final-live-dify-fixed-20260908 --user-id demo-user-001
```

结果：

- 聚焦测试 `14 passed`。
- 真实 Dify -> FastAPI/ChatService -> MySQL 订单查询通过。
- `tracking_query`、订单号 `ORD1001`、工具调用和物流结果均正确。
- 当前验证覆盖 Dify v6 双阶段回答契约和完成态修复；历史提交编号仅作 Git 记录，
  不得替代当前数据库事实。

### 3.3 知识库和 RAGFlow

执行：

```powershell
poetry run python evaluation/evaluate_knowledge.py
```

结果：

```text
5/5 passed
```

五个案例均使用 `ragflow` 后端并校验了结果内容和来源。无关问题返回空结果；
FastAPI `/api/v1/knowledge/query` 在上游错误时返回明确降级上下文。

### 3.4 工单状态和 MCP

- 工单状态聚焦测试与真实链路已通过：缺少 `ticket_no` 不调用工具，本人可查，
  他人和不存在工单返回统一安全错误；纯工单号在待处理 `ticket_status` 上下文中
  即使被 Dify 判为 `unknown`，Python 也能安全恢复并继续查询。
- MCP 聚焦测试：`24 passed`。
- 真实 MCP stdio 已逐项验证订单查询、工单查询、知识检索、地址修改、投诉和
  人工转接六个工具。
- 地址修改、投诉和人工转接的临时验收工单已清理。

### 3.5 MySQL、Redis 和备份恢复

- `/health`：成功。
- `/health/dependencies`：MySQL 和 Redis 均为 `ok`。
- 当前 MySQL 数据：`orders=3`、`tickets=117`、`tracking_events=5`。
- 备份脚本已生成：
  `data/backups/logistics_20260908_175619.sql`。
- 该备份已导入临时数据库并核对恢复数据，临时数据库随后已清理。

### 3.6 正式 50 条意图回归

最新正式报告已覆盖全部 50 条用例，50 条均有预测，执行失败 0 条，
`unsafe_tool_calls` 为 0。基础集 35 条中 intent 为 35/35、entities 为
32/35、missing_slots 为 34/35、tool_gate 为 34/35；对抗集 15 条中
intent 为 14/15、entities 为 14/15、missing_slots 为 13/15、tool_gate
为 15/15。`intent-017` 仍漏抽 `complaint_content`，所以不能描述为所有字段全通过。

## 4. 当前风险

1. `intent-017` 的投诉内容抽取仍不稳定；Dify/RAGFlow 请求重试、结构化观测
   和告警尚未作为生产化能力完成。
2. 当前仍需要完成消费者端自然语言回答、手机号验证码登录和电脑网页。
3. RAGFlow 是外部服务，真实查询存在网络超时风险；FastAPI 已提供降级回复。
4. MCP 当前主要完成本地 stdio 和真实业务闭环验收，生产级 OAuth、远程部署、
   观测和限流不在当前第一阶段范围。
5. 工作区仍有用户或历史遗留 dirty changes。任何新提交只能包含当前切片文件。

## 5. 下一步

1. 对 Dify v6 进行投诉、人工、外部异常和更多多轮样本的真实回归。
2. 专项修复 `intent-017` 的 `complaint_content` 漏抽，并按错误类别最小复测。
3. 将开发演示手机号验证码替换为真实短信认证和生产级令牌。
4. 增加服务端最近会话索引、跨设备恢复、登录过期和权限失效处理。
5. 完成第一阶段消费者电脑网页的真实环境验收收尾。
6. 每个新改动仍按小切片、先说明验收标准、先聚焦验证、只提交相关文件的规则执行。

## 6. Git 阶段版本

最近提交：

```text
003ccfa 1.3（浏览器操作交接规范）
上一提交之前的阶段记录保留在 Git 历史中；本次提交完成后，以新的 HEAD 为当前版本。
```

下一阶段提交建议继续使用：

```text
3.8（第一阶段生产化收尾）
```

# 电商物流智能客服产品需求文档

> 项目全局工作规范见 `WORKFLOW.md`。新窗口必须与本文件、`ARCD.md`、`project.md` 一起阅读。

## 1. 产品目标

构建一个可运行、可验证、可扩展的电商物流智能客服 MVP。系统应能理解用户当前消息，维护必要的多轮会话状态，并在权限和业务条件满足时调用订单、工单或知识库能力。

产品重点不是让大模型直接操作业务数据，而是让 Dify、Python Agent 编排层和业务服务各自承担清晰职责：

- Dify 识别当前消息意图并输出结构化结果。
- Python Agent 解析、校验、合并会话状态并决定是否允许调用工具。
- 订单和工单服务作为业务事实来源。
- 知识库负责物流规则类问答。
- Redis 保存短期会话状态，MySQL 保存正式业务数据。

## 2. MVP 范围

### 2.1 支持的用户能力

1. 查询具体订单的物流位置、运输进度和物流轨迹。
2. 查询运费、时效、禁寄品、签收规则等物流知识。
3. 提交收货地址修改申请。
4. 提交投诉工单。
5. 请求人工客服并创建人工服务工单。
6. 查询已有工单的处理状态。
7. 在信息缺失时只追问必要字段。
8. 在地址修改等写操作执行前要求明确确认。

### 2.2 意图枚举

```text
tracking_query
knowledge_query
address_change
complaint
human_transfer
ticket_status
unknown
```

### 2.3 工具调用安全要求

以下情况不得调用订单、工单或知识库工具：

- 意图为 `unknown`。
- 置信度低于 0.70。
- 必填槽位不完整。
- 当前用户没有通过身份和资源归属校验。
- 地址修改尚未明确确认。
- Dify 输出无法通过结构化校验。

## 3. 业务规则

- 订单和工单只能由归属用户访问。
- 对外错误信息不得泄露其他用户是否拥有某个订单或工单。
- 工单状态使用：
  `pending`、`processing`、`completed`、`rejected`、`cancelled`。
- 工单状态流转必须经过服务层校验。
- 使用 `client_request_id` 保证写入操作幂等。
- Redis 不是订单或工单事实来源。
- 订单、物流轨迹和工单的正式数据以业务数据库为准。

## 4. 非目标

当前 MVP 不包含：

- 真实电商平台或快递平台的生产 API 对接。
- 自动扣款、自动赔付和自动支付。
- 完整的用户注册、JWT 或 OAuth 认证体系。
- 面向终端用户的正式前端。
- Celery 异步任务和高可用生产部署。
- 让 MCP 绕过会话上下文直接访问数据库。

## 5. 验收方向

- Dify 能稳定输出符合契约的结构化结果。
- Python 能正确解析结果并维护跨轮状态。
- 缺槽位、低置信度、越权和未确认写操作都不会误调用工具。
- 订单、知识库、工单和人工转接主链路均有自动化测试。
- MySQL、Redis 和健康检查在真实运行环境中通过验证。
- 每次小切片都有聚焦测试、必要的集成测试和清晰的 Git 版本名。
